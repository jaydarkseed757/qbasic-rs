# Test scratch output
tests/tmp/
tests/tmp-gfx/

# Graphics golden-test artifacts (the .txt checksums ARE committed; PPM dumps are not)
*.ppm

# Random-access record files written by test/demo programs at runtime
*.dat
HALLFAME.*

# Rust build artifacts
/target

# Generated transpiler output (created by build-all.sh)
/bin

# Compiled sample binaries (samples/ holds .rs source only)
/samples/*
!/samples/*.rs
!/samples/*.MD

# Excluded per request
CLAUDE.md

# Local Claude / agent session data
.claude/

# macOS
.DS_Store
