' CHAIN target: receives COMMON values POSITIONALLY (QB semantics).
' Run standalone it prints the type defaults (0 / empty).
COMMON SHARED score, msg$
PRINT "child score:"; score
PRINT "child msg: "; msg$
